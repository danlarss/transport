// elastic host
hosts = ({ host = "http://127.0.0.1";
           port = 9201; },
         { host = "http://127.0.0.1";
           port = 9200; }
        );

// curl timeout
timeout = 1;

// flush reaponse on every call
flush_response = true;